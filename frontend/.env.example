# Backend base URL (used ONLY by the Vite dev proxy).
# In dev, Vite proxies `/api/*` and `/ws/*` to this target by default.
BACKEND_URL=http://localhost:3000

# Client-side configuration (injected into bundle by Vite).
VITE_API_BASE_URL=/api/v1
VITE_WS_PATH=/ws
