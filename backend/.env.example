# Backend env example (copy to `backend/.env`)

# API
API_PORT=3000
FRONTEND_URL=http://localhost:5173

# Workspace storage
WORKSPACE_BASE_DIR=../.workspace
DEFAULT_DOCKER_IMAGE=frame-code/workspace-ubuntu:latest

# Agent integration (agent-runner via stdio JSONL)
# Point this to a *built* checkout of agent-runner (must contain `dist/index.js`).
AGENT_RUNNER_DIR=G:\\novosApps\\agentes\\agent-runner

# Default adapter used when the frontend doesn't specify.
# Options: `echo` (stub), `claude` (stub for now), `codex` (stub for now)
DEFAULT_AGENT_ADAPTER=echo

